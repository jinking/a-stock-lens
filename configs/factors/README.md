# Factor configuration

Factor definitions, formulae, null policies, and versions are deferred to the factor-engine implementation. Factors remain objective raw values; strategy interpretation and scoring stay separate.
