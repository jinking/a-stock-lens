# BLOCKED.md — 待裁决清单

> 依据：`docs/superpowers/plans/2026-09-18-research-baseline-implementation-plan.md`
> 任务 1.1 / 1.2 / 1.3，与 `...-next-stage-execution-plan.md` §三 全局约束。
> 本文件只列**需要所有者或更高权威裁决**的事。执行者按默认走完的部分不在此列。

## 一、阻塞三个任务本身的事项

**无。** 1.1 / 1.2 / 1.3 均按计划完成，验收命令全部退出码 0（canonical 拒绝
是设计要求的行为，不是失败），测试全绿且 skipped=0。

## 二、明确点名的"顺手活"——**不许做**，等裁决

以下都是本轮**故意不做**的。它们每一项都会改变业务结论，因此必须另立规格，
不能在"留基线"这轮里夹带进来。

| 项 | 现状证据 | 为什么必须等裁决 |
| --- | --- | --- |
| 补 9 只缺行业标的 | 研究池 2,303 只里有 9 只没有行业归属：`000592.SZ`、`000968.SZ`、`002679.SZ`、`300896.SZ`、`600158.SH`、`600185.SH`、`600938.SH`、`601888.SH`、`689009.SH` | 补法有两种（重新抓取 / 人工指派），选哪种决定了这 9 只的行业归属是不是"证据"。人工指派等于给未证实的映射盖戳 |
| 改流动性门槛 | `configs/universe.yaml` 当前 `min_average_turnover_20d = 1.5 亿` | §三 明确"保留当前 Universe 配置，不为了凑 2,303 只而调阈值" |
| 修估值 | 四榜里 `garp` 可打分 1 只、`value` 2 只 | 属第三步（估值补全）范围，且需要先批准简化口径 |
| 加速归一化 | `run_research_analysis` 会重复调用归一化（下一阶段计划 §四.3 已点名） | 属第二步存储迁移范围；在本轮改动会污染基线 |
| 给 `industry_trend` 填权重 | 未实现 | 下一步独立立项，先定口径再定权重 |

## 三、顺手发现、需要裁决是否处理（本轮未动）

1. **`ruff format --check .` 在开工前的地基上就是红的**，与本次改动无关：
   `tests/unit/test_bootstrap_sync.py:158` 一行超长，需要拆行。
   它不在本任务白名单里（"只提交本任务文件"），因此我**没有**格式化它。
   当前状态：`ruff check .` 全绿、`ruff format --check .` 报 1 个文件待重排、
   `mypy` 全绿、`git diff --check` 干净。请裁决是否单独修这一行。
2. **`data/watchlist/` 目录在本机不存在**（还没有 tracked 标的）。1.1 的清单用
   `scanned_roots` 如实记成 `present=false, files=0`，没有伪造空目录。若将来要求
   "五类根目录必须都存在"，那是另一条产品规则，需要显式批准。
3. **本机 `pytest` 的 `tmp_path` 夹具不可用**（落在系统临时根下被拒绝，
   `PermissionError`）。已有 `local_tmp` 夹具可用，本轮照它写测试。若希望脚本层
   测试也能用 `tmp_path`，需要先解决运行环境的临时目录策略。

## 四、与所有者的约定一致、但值得记一笔的边界

- **不 push、不合并、不改 CI**：本轮只做本地提交（`main` 分支），共 3 条提交。
- **`var/acceptance/**` 不入库**：`.gitignore` 已加一行 `var/acceptance/`；
  清单与六个分析产物都是本地证据，交付时以路径与 sha256 引用。
- **一个与本任务无关的既有未跟踪文件** `var/industry-map-2026-09-18.csv`
  （5,542 行的 `symbol,industry` 外部映射）保持原样：未改动、未入库、未删除。
