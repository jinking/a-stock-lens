.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
var/astock.duckdb
var/logs/*.log
data/**/*.parquet
# 归一化 Parquet 层与它的质量门报告同属运行时产物：`docs/STORAGE.md` 规定
# 这一层由 `NORMALIZE` 产出、可随时重建，重建的代价来自 Raw CSV，不来自版本库。
data/normalized/**
!data/normalized/.gitkeep
# Runtime artifacts: `docs/DEVELOPMENT.md` keeps `data/` and `var/` out of the
# repository, so snapshots, watchlist entries and job manifests written by a
# local run must not show up as pending changes.
data/snapshots/**
!data/snapshots/.gitkeep
data/snapshots_legacy_*/**
# 落地后的原始数据同样是运行时产物：`docs/DEVELOPMENT.md` 第 38 行规定 `data/`
# 不进版本库，而真实 `astock sync` 会把 daily_bars/securities 写到这里。
data/raw/**
!data/raw/.gitkeep
data/watchlist/
var/jobs/
var/jobs_legacy_*/**
# 迁移前基线的验收产物（输入清单、六个分析产物、命令日志）是本地证据，
# 按 `docs/DEVELOPMENT.md` 的口径与 `data/`、`var/` 一样不入库。
var/acceptance/
var/calibration/
# 全市场冷启动的实测产物：基准 JSON、子集数据根与运行日志都属运行时，不入库。
var/bench-*/
var/benchmarks/
var/*.log
# WeStock CLI 是外部二进制（8.6MB，可从腾讯官方发布地址复现获取），
# `README.md` 明确它不随仓库分发；默认路径由 provider 定为 tools/bin/westock。
# 只留目录占位，二进制本身不入库。
tools/bin/*
!tools/bin/.gitkeep
tests/.tmp/
