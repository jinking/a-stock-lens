# 2026-09-20

## Candidate Readiness 升级：任务 1（基线记录与估值时点可见性钉住）

- 启动 HEAD：`dd45d1de77b80229447822b57d4f8de5b104c87b`
- 测试基线：`uv run pytest -q` -> 977 passed, 10 warnings
- 静态检查：`uv run ruff check .`、`uv run mypy` 均 clean
- 时点隔离（PIT）验证：
  - 在 `tests/unit/test_normalized_repository.py` 增加 `test_valuation_inputs_point_in_time_isolation` 用例；
  - 证明早于 2026-09-19 的时点绝不泄漏读取 2026-09-19 估值数据，且同一标的指标值严格隔离；
  - 验证 `data/raw/neodata/valuation/2026-09-17.csv` 未被任何操作修改；
  - 聚焦测试 `tests/unit/test_normalized_repository.py` 12 passed。

## Candidate Readiness 升级：任务 2（估值补齐后的只读研究分析基线固化）

- 修复事实：UniverseBuilder 增加非交易日/休市日自动对齐最新有效交易日逻辑（解决周六 2026-09-19 全池被误判为停牌 NO_MARKET_DATA 导致 0 标的 Bug）。
  - 用户明确指示：后续将休市日单独列出，并在休市日跳过抓取。
- 研究池标的：2,303 只
- 因子产物：55,272 条，六策略产物：13,818 条
- 六策略可打分与排名统计：
  - growth: evaluable=2,302, ranked=2,302
  - momentum: evaluable=2,281, ranked=2,281
  - quality: evaluable=1,697, ranked=1,697
  - dividend: evaluable=1,612, ranked=1,612
  - value: evaluable=1,578, ranked=1,578（新估值生效）
  - garp: evaluable=849, ranked=849（新估值生效）
- 产物哈希固化在 var/acceptance/post-valuation-20260919/analysis/manifest.json。

## Candidate Readiness 升级：任务 3（集成测试等价链路）

- 编写 `tests/integration/test_post_valuation_analysis_flow.py`，完整覆盖研究池筛选、因子计算、六策略打分与导出流程。
- 保证只读分析逻辑与日常调度生成的快照逻辑保持逐条等价。

## Candidate Readiness 升级：任务 4（正式快照发布与等价性验证）

- 执行 `astock daily --as-of 2026-09-19 --allow-incomplete`，成功写入 `FACTOR`, `UNIVERSE`, `STRATEGY` 正式快照。
- 验证 `data/snapshots/CANDIDATE/2026-09-19.json` 依法未生成；`GET /candidates?as_of=2026-09-19` 返回 404；`BUILD_CANDIDATES` 保持 `BLOCKED`。
- 比对任务 2 只读分析结果与任务 4 快照数据：STRATEGY (13,818)、UNIVERSE (2,303)、FACTOR (55,272) 完全等价。

## Candidate Readiness 升级：任务 5（六策略选股榜验收与零突变）

- 验证六策略 `astock screen <strategy> --as-of 2026-09-19 --top 20` 正常输出（growth 2302, momentum 2281, quality 1697, dividend 1612, value 1578, garp 849）。
- 确认全流程对快照、自选股、作业状态无任何修改（零突变）。
- 抽查 Value 榜首 `601336.SH` 与 GARP 榜首 `000688.SZ` 单股画像，估值因子与策略评分均真实生效，`candidate_status="not_published"`。
- 更新 `docs/ROADMAP.md` 与 `docs/REMAINING_PRODUCT_BLOCKERS.md`。

## Candidate Readiness 升级：任务 6（行业覆盖核验与 Blocker 保持）

- 核验 2,303 只研究池标的，行业覆盖 2,294 只（99.61%），缺失 9 只。
- 确认 9 只缺失标的系上游源端 `data/raw/westock/industry/2026-09-18.csv` 遗漏，非解析器缺陷。严禁静默伪造。
- 保持 `IndustryCoverageUnavailable` 阻断，符合 100% 行业覆盖安全契约。

## Candidate Readiness 升级：任务 7（诊断校准包与决策材料生成）

- 导出诊断行业映射 `var/calibration/industry-2026-09-18.csv`。
- 运行 `--industry-map ... --diagnostic-only`，产出 `var/calibration/2026-09-19-candidate-calibration.json` 与 `.md`。
- 撰写决策材料 `docs/decision-packets/2026-09-20-candidate-qualification-diagnostic-packet.md`。
- 记录四大核心因子异象：
  1. 成长净利同比极值基数异象（高达 415,612%）；
  2. 股息支付率超额异象（最高达 9282%）；
  3. PEG 源端量纲偏高异象（中位数 166.27）；
  4. 价值榜首全为保险央国企。
- 将 `var/calibration/` 纳入 `.gitignore`。

## Candidate Readiness 升级：任务 8（全量验证与所有者停步门禁）

- 代码质量：`ruff check .`、`ruff format --check .`、`mypy`（116 文件）全绿。
- 测试回归：`make test-fast PYTHONPATH=` 977 passed, 10 warnings。
- 严格核验安全边界：
  - `configs/qualifications/` 目录严格保持不存在；
  - 无任何跨策略伪合成评分字段；
  - 停步并输出强制所有者决策门禁报告，等待产品所有者对行业缺口处理与策略资格门槛的明确决策授权。

## 所有者正式决策批准（Owner Sign-off 2026-09-20 10:45）

- 用户指令：“按照你的推荐来”。
- 明确裁决：
  1. **策略门槛方案**：正式采纳【方案 1：稳健平衡型规则】作为六策略绝对质量门槛标准（Growth: 净利同比>=15%、营收同比>=5%、ROE>=8%；Dividend: 股息率>=3%、支付率 10%~80%；Value: PE<=25、PB<=2.5、ROE>=5%；Quality: ROE>=12%、毛利>=20%、资产负债率<=65%；Momentum: 创一年新高附近>=0.80；GARP: PE<=35、净利同比>=15%、ROE>=10%）；
  2. **行业覆盖门禁**：坚持 100% 覆盖硬性要求，不进行假行业兜底；待在数据层补齐 9 只缺失标的二级行业后，正式解锁配置落地通道；
  3. **休市日日历跳过**：作为最高优先级开发任务立即安排排期落地。

## 休市日交易日历与跳过机制 TDD 落地（2026-09-20 10:56）

- 遵照 Superpowers（Brainstorming -> TDD）流程完成：
  - 新增 `astock_lens.calendar` 核心模块：`ChinaTradingCalendar` 内置 2024~2027 年法定节假日与调休规则，提供交易日判定与对齐；
  - 数据落地层门禁：`land_raw` 在休市日彻底跳过 `daily_bars` 网络抓取，返回 `NOT_APPLICABLE`；
  - CLI 命令层：新增 `astock calendar is-open` 与 `latest` 子命令，`sync-research` 休市日自动跳过行情抓取；
  - 测试套件全量通过：`make test-fast` 从 977 增至 986 passed（0 failed, 10 warnings），静态检查全绿。

## 补全研究池 9 只标的申万二级行业映射与 100% 覆盖门禁解除（2026-09-20 11:15）

- 遵照 Superpowers（Brainstorming -> TDD）流程完成：
  - 根因查明：上游 WeStock CLI `sector ranking --kind industry` 仅列出了 124 个板块，遗漏了申万 2021 二级分类中的 10 个细分板块（林业Ⅱ、油气开采Ⅱ、旅游零售、体育Ⅱ、医疗美容等），九号公司（689009.SH CDR）等亦受影响；
  - 权威行业映射核验：完成平潭发展/福建金森（林业Ⅱ）、蓝焰控股/中国海油（油气开采Ⅱ）、爱美客（医疗美容）、中体产业（体育Ⅱ）、珠免集团（房地产开发）、中国中免（旅游零售）、九号公司（摩托车及其他）权威核验，获得所有者确认；
  - 配置文件落地：`configs/industry_supplement.yaml` 显式记录 9 只标的官方行业代码、名称与证据版本，零造假、零网络波动；
  - 映射装配与防歧义：在 `src/astock_lens/data/industry.py` 实现 `load_supplemental_industry_memberships` 与环境变量覆盖，严格维持 `IndustryMembershipAmbiguous` 防歧义红线；
  - 命令行无缝合并：在 `astock industry export-map` 与 `astock calibrate candidates` 中自动合并补充映射；
  - 门禁正式解除：真实运行 `astock calibrate candidates --as-of 2026-09-18`（不加 `--industry-map`），研究池 2,303 只标的行业覆盖率达到 **2,303 / 2,303 (100.0%)**，`origin="canonical"`，`IndustryCoverageUnavailable` 异常完全消除；
  - 全量回归：`make test-fast` 达 **990 passed**（新增 4 个测试用例，0 失败），`ruff` 与 `mypy` 全部 Clean。
