# 2026-09-22

## Web MVP 后续任务

- 完成 Stock Profile 与 Strategy 页面：只读调用既有 API，保留因子缺失状态与候选血缘，不在前端补算排名或资格。
- 完成 Web 响应式样式、可访问性测试、Node 22 CI job 和本地浏览器验收。
- 本地正式快照 `2026-09-17`：Candidate 共 50 只；Today、Candidates、CLI 与 API 前十顺序一致；Value Strategy 排名 API 覆盖 `2/2303`，Qualified 共 1 只。
- 浏览器访问前后快照、Watchlist、Job 文件哈希未变。验收使用 JSON 存储后端；Playwright 路由覆盖 `/`、`/candidates`、`/stocks/000001.SZ`、`/strategies/value`。
- `.github/workflows/ci.yml` 已加入 Web job。PR #3 的首轮 Web job 通过，Python job 因 CLI 基准日线路径没有跟随 `ASTOCK_CSV_ROOT` 失败；Jev 分类后已修复该实现问题。
- 最终复审修复覆盖度缺失被静默归零、策略响应竞态与 Qualified 门槛字段臆断；最终 Web 回归为 9 个测试文件、65 项通过，typecheck/build 通过。
- 修复后本地 Python 全门禁通过：ruff、格式、mypy、pytest 1245 项。等待修复提交推送后的 PR CI 通过，再将 PR #3 合并入 `main`。

## Trade Gate V1 增量实现（部分完成，2026-09-22）

- 不复用 SnapshotStore：它以 `(kind, as_of)` 唯一，无法表达同日多次评估；Trade Ledger 按记录 ID append-only。
- 不复用 Watchlist `HOLDING`：它是预留状态且 V1 不得触达；交易执行与复盘归属独立 Trade Gate 生命周期。
- 不把历史日线当实时行情：EVENT/SWING 的交易时点输入只能来自显式 `TradeMarketOverlay`，缺失就保留缺失。
- AI 两阶段审计：第一阶段 payload 不包含 thesis；第二阶段才获得独立评估与 thesis。AI 不产生最终 PASS。
- Profile 评分、80/70 决策分界、Veto 与更小仓位 Override 约束已实现；未增加账户风险上限。
- 当前实现仅部分完成，未完成 Golden Scenario、完整 CLI/API/lifecycle 验收与文档原文归档，不能宣称交付完成。
- 旧链路只读冒烟：`astock today --as-of 2026-09-19`、`astock candidates --as-of 2026-09-19 --top 20`、`astock stock 001309.SZ --as-of 2026-09-19` 均退出 0；`data/snapshots/` 全部文件 SHA256 前后完全一致。

## 复杂度收敛计划执行（2026-09-22）

- 已将 v3 设计规格与 v2 实施计划纳入 `docs/superpowers/`。计划引用的前一版复杂度计划与 ownership matrix 未在仓库或下载输入中找到；Task 1 仅据 v3 规格和当前计划明确项建表，并记录该限制。
- 原基线提交 `fe09b004...` 已过时；执行中 `main` 又前进至 `11265235342e01aeb5f94ae1f874f543dc1b157f`。在 Task 2 生产改动提交前用干净 worktree 重采，最新基线 1,274 个 Python 用例通过，Ruff/mypy、Web 65 项测试、typecheck/build、Trade Gate 专项均通过。
- Task 2 将六个重复策略资格器合并为 `ConfiguredQualifier`，保留六个旧模块的兼容 wrapper；移除一个独立构造测试，把策略 ID 覆盖并入注册表成功测试。提交 `d375006`；定向资格测试 53 项通过，Ruff 与 mypy 通过，收集数 1,273。
- 工作区仍有 `.env.example`、`AGENTS.md`、`README.md`、Jev 示例及 Westock fixtures 的既有未提交内容；保持原样。Trade Gate 在本计划中冻结，未由 Task 2 修改。
- Task 5/6 有证据测试收敛：候选拒绝独立测试与最高分参数例输入完全相同，Task 6 复核时合并；路由移除纯函数自比较并把 5 个未使用 score 参数合并为 API 缺席用例，删除被逐因子缺失测试覆盖的独立流动性缺失用例。Task 5 定向单测/集成共 37 passed，Task 6 七集群相关回归 385 passed。Task 6 前全量收集 1,265，合并第四项完全重复断言后为 1,264（相对 1,274 基线少 10，含 Task 2 减少 1）。
- Task 3/4 已执行：在保留 `cli/trade.py` 与 Trade Gate 挂载不变的前提下，拆分发现/研究生命周期、数据、管线与校准 CLI 模块。Task 4 CLI 回归 160 passed，根命令及七个子命令帮助输出与基线一致。
- Task 5/6 独立代码审查结果：APPROVE，无发现；审查确认被删断言为重复或恒真。Task 6 的 385 项集群回归发生在最后一个完全重复测试删除之前；删除后相关三个单测文件 22 项通过、全量收集 1,264。最终完整 pytest 仍由 Task 8 统一验收。

## 复杂度收敛最终验收（2026-09-23）

- Task 7 已完成：架构文档明确 Stock Discovery / Research Context 与 Trade Gate Context；Trade Gate 消费研究事实、不重算因子/策略、不进入 daily，`TradeDecision` 不改变 Candidate admission。README 与 PROGRESS 不宣称完整交易评估/执行/复盘已完成；WeStock 规格改以 `cli/runtime.py::_bulk_provider` 为目标，并加入 Test Delta Budget 与契约测试复用规则。
- Task 8 Python 验收：全量 `pytest` **1264 passed / 9 warnings**；测试收集 **1264**（低于 1274 基线）；ruff、格式、mypy 全绿；Trade Gate 三个冻结测试 **8 passed**；Artifact tests **60 passed**。
- Task 8 Web 验收：`npm ci` 后 Web tests **65 passed**，typecheck 与 build 通过。CLI 帮助输出与基线一致；`configs/trade_gate`、`src/astock_lens/trade_gate` 相对 `1126523` 无差异，JobStage/daily 未新增 Trade Gate 引用。
- 为通过最终格式/类型门禁，修正一处既有资格测试格式与两处 CLI 类型兼容问题；无行为变化，提交 `0426aab`。
- 最终独立代码审查为 **APPROVE**：八项边界逐条通过；审查补充确认实际净减少为 10 个测试（不是早期文档中的 9 个），已修正 `docs/superpowers/reviews/2026-09-22-test-ownership-audit.md`，提交 `9b15b94`。
