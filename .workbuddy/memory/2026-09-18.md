# 2026-09-18 工作记录

## 一、计划接入：校准就绪 / 研究工作池（Calibration Readiness）

所有者提供并入库：`docs/superpowers/plans/2026-09-18-calibration-readiness-implementation-plan.md`
（提交 `4d9d87e`「计划：加入校准就绪与研究工作池实施计划」）。本轮按该计划
Task 1 → 11 严格执行，每个 Task 单独测试、中文 commit、推送远端。

计划已经写明的所有者裁决（本轮不再重新讨论）：

- 全交易所名单（约 5,500 只）继续作为轻量主数据；
- 昂贵处理只跑**研究股票池**，目标约 2,000–3,000 只；**2,500 是目标中心，不是硬配额**；
- 研究池筛选必须与策略无关（不得用六策略打分决定入池）；
- 校准总体必须与正式策略扫描总体一致（不得用 5,500 只校准、再对 2,500 只生产）；
- 冷启动允许为宽名单取"最小流动性启动历史"，因为 `avg_amount_20d` 必须先可测；
- 正式 Candidate 发布在校准规则批准前保持 `BLOCKED`。

## 二、执行方式的一个偏差（必须留档）

所有者要求用 Superpowers `subagent-driven-development`（每任务独立实施者 + 独立评审者）。
**本环境子代理载荷投递不可用**：`spawn_agent` 与 `followup_task` 各派发一次，子代理都回报
"未收到任务载荷"（与 2026-09-17 记录的同一故障一致）。

**裁决**：改为由主控在本会话内逐任务执行，保留每任务的需求简报文件、TDD 的 RED/GREEN 证据、
简报指定的验证命令、逐任务 commit+push 与账本（`.superpowers/sdd/2026-09-18-.../progress.md`）。
**代价**：SDD 要求的独立评审者座位（任务级 spec/质量评审、最终整支评审）无法由独立上下文承担，
改为主控自审 + 每任务全绿验证；该偏差已写进账本，并会在本轮最终报告里显式声明。

## 三、Task 1：研究池前置筛选边界（提交见下）

- 新增 `src/astock_lens/universe/prefilter.py`：`PrefilterResult`（`included` / `excluded`）
  与 `prefilter_listing(securities, *, config, as_of)`。
- **边界裁决**：前置筛选只判定上市元数据可回答的规则——交易所、ST、退市板块、上市年龄；
  明确**不**判定流动性、当日是否有行情、停牌天数、以及任何策略分数。缺行情/缺流动性度量的
  标的照样通过前置筛选，由后面有证据的层判它。
- **不复制规则语义**：把 `UniverseBuilder._evaluate` 里的五条规则抽成模块级单规则函数
  （`exchange_exclusion` / `st_exclusion` / `delisting_board_exclusion` /
  `long_suspension_exclusion` / `short_listing_exclusion`），`BUILDER_RULE_CHECKS` 固定顺序；
  前置筛选按 `LISTING_RULE_CHECKS` 复用其中四条。两条一致性测试（逐规则判定相同、
  与 builder 在真实 fixture 上清单判定完全一致）就是"不许长第二份语义"的守门人。
- `_require_timezone` 提升为公开的 `require_timezone`，供前置筛选复用，避免出现第三份弱守卫。
- 顺序保持：快照里排除项的产出顺序仍是 EXCHANGE → ST → DELISTING_BOARD → LONG_SUSPENSION →
  SHORT_LISTING（抽函数时逐条对齐，未改变既有输出顺序）。

## 四、文档漂移修复

- `docs/ROADMAP.md`：`d964422 / 674 tests / 3 只真实数据` 这组说法作废。基线改为实测值——
  本轮起点 `4d9d87e`、`uv run pytest -q` 实测 `734 passed`；真实数据现状改为 5 只
  （`000001.SZ`、`300750.SZ`、`600519.SH`、`601318.SH`、`688981.SH`），日线 22,693 根
  （1991-04-03 至 2026-09-17），全市场名单实测 5,565 只。
- `configs/factors/README.md`：从陈旧的 4 因子表补成与 `configs/factors/*.yaml` 一一对应的
  24 个因子（按 domain 分组），并单列出**未批准**项：全部 `stale_after_days: null`、
  因子合格线未批准、Growth 极值 / Dividend 支付率形状 / PEG 值域待裁决。

## 五、环境：superpowers 落地情况（非代码改动）

- 查清了「superpowers 到底装在哪」：**WorkBuddy 此前未装**；Claude Code 与 Codex 各装了
  原版 `obra/superpowers` **6.3.0**（14 个技能，带 SessionStart hook / commands / agents）。
  本仓库 `docs/superpowers/{specs,plans}/` 的目录约定来自那套原版工作流，`AGENTS.md` 亦将其
  列为第一权威来源——即"没装在 WorkBuddy，但一直在用它的产物规范"。
- 本次为 WorkBuddy 装了**推荐市场（BuiltinMarket）版**：`superpowers` v1.0.0，落到
  `~/.workbuddy/skills/superpowers/`，6 个纯 Markdown 文件（254 行，36K），无脚本。
  内容是 obra 方法论的**自包含中文重制**（7 步基础工作流 + 5 篇 references），不含源码搬运。
- 安全审计结论：**P2 安全**。危险关键词（curl/eval/subprocess/sudo/rm -rf/凭证路径/dev/null
  重定向）全域零命中，零外链，无可执行文件。
- 注意：这与原版插件**不是同一产物**。原版 `superpowers@codebuddy-plugins-official`（4.0.3）
  在本机插件市场里也有货，但需从插件面板安装；本轮未装（改 `installed_plugins.json` 属手术式
  操作，且 CLI `plugin install` 在本环境下会挂起）。WorkBuddy CLI 产物同时支持
  `CLAUDE_PLUGIN_ROOT` 与 `CODEBUDDY_PLUGIN_ROOT`，故原版 hook 理论上可用。
- 结论留档：**已装**（市场版），**未装**（原版插件），两者可并存。

## 六、Full-market bootstrap redesign Task 2

- 新增 `data/bootstrap_sources.py`：批量请求/结果模型、批量来源协议、单标的 fallback 协议。
- 批量结果显式携带 `missing_symbols`；空 `VALUE` `RawDataset` 被拒绝；`as_of` 必须带时区。
- 按所有者硬约束未派发子 Agent/Reviewer、未接 provider/CLI/scheduler、未运行全市场，主控完成 TDD RED/GREEN 与自审。
- 提交：`4982d3a`「数据：建立批量优先与单标的补缺的启动数据源契约」。
- Task 2 fix round 1：新增 `validate_bootstrap_as_of`、`validate_bootstrap_source_dataset`、
  `validate_bootstrap_batch_result`，明确 Protocol 消费边界，补齐 fallback naive 时间与空 VALUE 反例；
  提交 `2e22d6a`「修复：补强启动数据源运行时校验边界」。
- Task 2 fix round 2：新增 `ValidatedSymbolBarFallbackSource` 强制适配器，调用前校验时区、返回后
  校验 dataset，保留原 Protocol 方法签名；11 个聚焦测试、mypy、ruff、diff 全绿；提交
  `14a3f25`「修复：增加启动补缺数据源强校验适配器」。

## 七、Full-market bootstrap redesign Task 3

- 仅做本地只读探测：当前 worktree 的 `.venv` 无 AkShare，锁文件候选版本为 `1.18.94`；
  `tools/bin/westock --help` 报二进制不存在，未执行联网请求。
- 已实现 AkShare 日线端点 `stock_zh_a_hist_tx` 是逐标的调用；WeStock 的批量能力只覆盖财报，
  neodata 不提供日线 OHLCV/amount。
- 结论：`NO_BATCH_PRIMARY_AVAILABLE`；Task 8 只能消费单标的 fallback，不能新增或臆造批量行情 Provider。
- Task 3 fix round 1：日线请求数改为“未观测（静态 transport 调用形状推导）”，未安装模块的
  行数/请求数改为“未观测／不适用”，报告补录 uv 缓存权限错误；结论不变。

## 八、Full-market bootstrap redesign Task 4

- 当前 `_live_transport` 直接调用 AkShare endpoint callable，未取得连接/读取 timeout 或取消句柄；
  `Future.result(timeout=...)` 仅限制调用方等待，不能中止底层 I/O。
- 选择 Path B：导出并由 `AkShareProvider.fallback_execution_requirement` 暴露
  `AKSHARE_FALLBACK_REQUIRES_PROCESS_ISOLATION`；后续 fallback scheduler 必须按单次 live 调用
  使用可终止、可回收的 worker process，不能以线程池/Future timeout 代替。
- 新增不联网探针：永不返回的假 AkShare endpoint 在子进程内直接经过 `_live_transport` 调用；
  0.15 秒 deadline 后父进程终止并回收，最终实测 0.157 秒（上界断言 < 1 秒）。
- TDD RED 为缺失常量的 ImportError；最终 `test_akshare_provider.py` 16 项、ruff、format、mypy 与
  `git diff --check` 均通过。提交 `c385839`「数据：声明 AkShare 回退需进程隔离」。未联网、未跑全市场，
  未触碰 Task 10/11/12 Gate。

## 九、Full-market bootstrap redesign Task 5

- 新增 `data/bootstrap_scheduler.py`：`fetch_symbols_bounded` 以完成顺序回调 `FallbackAttempt`，并用
  严格 active set 限制 `max_inflight`；所有 source 都经 `ValidatedSymbolBarFallbackSource` 消费边界。
- 消费 Task 4 的 `AKSHARE_FALLBACK_REQUIRES_PROCESS_ISOLATION`：`AkShareProvider` 路径每个调用单独
  使用 worker process。deadline 后父进程 `terminate()` 且 `join()`，测试用真实 Provider 类型加本地阻塞
  transport 验证无存活 child；deadline 是 scheduler wall-clock 边界，绝非 transport timeout。
- 裁决：若一组 active worker 全部无完成而超时，先排空该 timeout cohort 并停止继续提交，避免将 source
  整体不可用扩张为 `N * timeout`；未提交标的保留给上层 checkpoint 后续 invocation，绝不伪造成成功或 0。
- 测试覆盖慢首快后完成顺序、5,300 fake 标的上限、全挂 AkShare process 回收、成功请求上界；未联网、未跑全市场，
  未触碰 Task 10/11/12 Gate。

## 十、Full-market bootstrap redesign Task 6

- 新增 `data/bootstrap_checkpoint.py`：`BootstrapSymbolState` / `BootstrapManifestEntry` / `BootstrapManifest`、
  按标的的分片 `bootstrap/<as-of>/parts/<symbol>.csv`、原子（temp + replace + fsync）写的 `manifest.json`；
  成功跨对象重建可见、续跑只暴露未成功标的、重复成功只留一份分片与一条清单记录（Steps 1–4，`93eba0b`）。
- Task 6 Step 5 编排接入：`land_bar_chunks` 的"每块 `write_merged(daily_bars.csv)`"改成逐标的经
  checkpoint 落分片；整份文件改为**每次落地调用合并一次**（来源是本轮全部已落盘分片，列并集 + 行按分片顺序）。
- 续跑判据 = 清单 `SUCCESS` 且分片覆盖本次窗口；窗口加宽后不覆盖，扩展抓取照常发生（续跑不会挡掉补历史）。
- 失败语义：超时 → `TIMEOUT`、来源错误 → `SOURCE_ERROR`、空数据 → `EMPTY`，都带原因；绝不落空行、绝不写 0。
- RED：合并次数测试 `assert 3 == 1`（3 块 3 次合并）；其余新测试因缺 `checkpoint` 参数 TypeError。
  GREEN：四文件 35 passed（原 31 + 新增 4），全量 771 passed / 7 skipped，ruff/format/diff-check 与定向 mypy 通过。
- 发现（交给 Task 7/9）：`record_success` 每只标的原子重写整份 manifest，实测 5,300 只 84.4 秒、manifest ≈0.97 MB，
  属 O(N²) 写放大，建议改为每块批量 flush 或按标的清单分片。
- 未联网、未跑真实全量；未触碰 Task 10/11/12 Gate；Task 13 未执行。

## 十一、Full-market bootstrap redesign Task 7

- `compact_bootstrap_run(*, checkpoint, canonical_path)`：按清单里 SUCCESS 的 symbol 排序读分片，
  折成一份后把整份 `daily_bars.csv` 只读写一次并**原子替换**（临时文件 + os.replace + fsync）。
- `sync.py` 抽出唯一合并实现 `_merge_into_file(..., atomic=)`，新增 `write_merged_atomic` 与
  `merge_payloads`；既有 `write_merged` 保持同键替换语义与字节不变，其它落地流程不受影响。
- 三条性质有测试：落盘顺序不影响结果（逐字节相同）、压实幂等、同键替换且不动未覆盖的行。
- canonical 合并路径收敛：`land_bar_chunks` 收尾改调 `compact_bootstrap_run`，删除 Task 6 临时
  在 bootstrap.py 里的 `_union_payloads`；失败标的的分片不参与合并。
- RED：3 项 `AttributeError: ... has no attribute 'compact_bootstrap_run'`（模块属性访问，不打断收集）。
  GREEN：四文件 38 passed，全量 774 passed / 7 skipped，ruff/format/diff-check 与定向 mypy 通过。
- 未联网、未跑真实全量；未触碰 Task 10/11/12 Gate；Task 13 未执行。manifest 写放大仍待 Task 9 处理。

## 十二、Full-market bootstrap redesign Task 8

- 编排入口签名收口为计划规定形状：`bootstrap_liquidity_history` / `bootstrap_strategy_history`
  现在收 `batch_source: BatchMarketBarSource | None` + `fallback_source: SymbolBarFallbackSource`。
- `land_bar_chunks` 变成"一轮落地"：批量优先（按 `batch_size` 分批，逐 symbol 落分片，只接受本次
  请求过的标的）→ 只对缺口走 `fetch_symbols_bounded`（有界 in-flight、完成顺序、先落盘再提交下一只）
  → 收尾经 Task 7 的 `compact_bootstrap_run` 只压实一次。
- 尊重 Task 3 的 `NO_BATCH_PRIMARY_AVAILABLE`：不新增批量 provider，CLI 显式 `batch_source=None`。
- `FallbackAttempt` 新增 `timed_out`，scheduler 的 deadline 与来源报错在清单里分成 TIMEOUT/SOURCE_ERROR；
  删除 bootstrap.py 的 `SymbolBarFetcher`/`_fetch_chunk`/`_SymbolFetch`/`_timeout_dataset`（提交-全等-按序取
  的旧路径彻底消失）。`SymbolBarFallbackSource` 变 runtime_checkable，供 CLI 在接线处拒绝不合格 provider。
- CLI：`--workers` → `max_inflight`；`--chunk-size` 语义改为"一次批量请求最多覆盖多少只标的"。
- RED：5 项编排测试 `TypeError: ... unexpected keyword argument 'batch_source'`。
  GREEN：五文件 54 passed，全量 779 passed / 7 skipped，ruff/format/diff-check 与定向 mypy 通过。
- 未联网、未跑真实全量；未触碰 Task 10/11/12 Gate；Task 13 未执行。

## 十三、Full-market bootstrap redesign Task 9

- 新增 `data/bootstrap_progress.py`：`BootstrapProgress` / `ProgressSink` / `render_progress` /
  `BootstrapProgressTracker`（15 秒心跳、收尾必发、throughput 只用本次实测值、`now` 可注入做假时钟）。
- 进度来自执行路径本身：`fetch_symbols_bounded` 新增 `on_inflight`；`land_bar_chunks` 在批量覆盖、
  每只补缺完成、in-flight 变化时上报；`_extend_history` 每轮以**实测** bar 数更新 satisfied。
  CLI `sync-bootstrap` / `sync-research` 各自打印一行心跳；集成测试断言输出含 `processed 40/40` 与 `sym/s`。
- `scripts/watch-bootstrap.sh` 改为从本次运行的 manifest 推导 as-of / 所需 bar 数 / 规模，删掉写死的
  `TOTAL=5301`、`REQUIRED=20` 与日期，并注明命令自身心跳才是主要观测手段。
- 顺带修掉上轮携带项（清单写放大）：`BootstrapCheckpoint.flush()` + `record_*(..., flush=False)`，
  攒够 `MANIFEST_FLUSH_EVERY=64` 条才原子重写；实测 5,300 只 80.8 秒/5,300 次写/2,571 MB →
  4.4 秒/83 次写/40.8 MB。
- RED：4 项（三条 flow 测试缺 `progress` 参数、watcher 仍写死市场只数）；GREEN：进度 9 passed、
  全量 790 passed / 7 skipped，ruff/format/diff-check 与定向 mypy 通过。
- 诚实记录：清单攒批那两项测试是实现后补写的（无预实现 RED），用"阈值=1 时 70 只写 70 次、断言失败"
  做了敏感性验证。
- 未联网、未跑真实全量；未触碰 Task 10/11/12 Gate；Task 13 未执行。

## 十四、Full-market bootstrap redesign Task 10（硬门禁）——PASS

- 新增 `tests/stress/test_bootstrap_scale_properties.py`：5,300 只假源、走真实编排路径，证明
  ①全成功收敛且请求数 = 标的数、零重复；②1%/1%/1% 超时/来源错误/空历史下成功不丢、失败显式、
  循环收敛；③断点续跑不重抓且最终文件与一次跑完逐字节相同；④批量覆盖 90% 时补缺恰好只剩缺口。
- 实测：全成功 9.0s/5,300 请求/并发峰值 8/canonical 106,000 行；混合失败 11.7s/5,322 请求/
  satisfied 5,141/failed 159（53+53+53）；批量 7.5s/6 次批量 + 恰好 530 次补缺。
  聚焦 4 passed in 52.13s；全量 794 passed / 7 skipped（80.46s）；ruff/format/diff-check 通过，
  `mypy src/` 只剩 HEAD 既有的 duckdb 缺失。
- 非空洞性：并发断言上下界一起写；发现"假源瞬时返回 → 线程不重叠、峰值=1"，给假源加 2ms/次耗时后
  峰值才到 8；失败注入比例与批量覆盖面都显式断言。
- 发现（记录在案）：不可终止源下，被放弃的超时调用仍在线程里跑，活调用峰值可短暂超过 max_inflight
  （观测 10 > 8）；AkShare 进程隔离路径的严格上界由 test_bootstrap_scheduler.py 钉住，调度器的
  "整批超时且无完成即停止提交"防止无限增长。
- Ruling：Task 10 完全通过，Task 11（真实 100 只）/ Task 12（真实 500 只）允许执行；
  **Task 13 全市场仍禁止**。Task 11 前提：本 worktree 无 AkShare，需合入 main 后跑或 `uv sync
  --extra providers` 后用独立 ASTOCK_CSV_ROOT 跑，均需所有者确认后再动网络。

## 十五、全市场冷启动重构：Task 10 假源门禁 + 真实 100 只门禁（均 PASS）

- 分支 `full-market-bootstrap-redesign` 已 fast-forward 合入 main（main `1e966a7`），后续在 main 上继续。
- Task 10：`tests/stress/test_bootstrap_scale_properties.py` 四项规模属性（全成功 5,300 请求零重复、
  1%/1%/1% 混合失败、断点续跑逐字节一致、批量覆盖 90% 只补缺口）聚焦 4 passed in 52s。
  两条口径说明：并发断言必须写上下界（瞬时返回的假源实测峰值只有 1，会放过串行实现）；
  不可终止源下活调用会短暂超过 max_inflight（实测 10 > 8），AkShare 子进程路径的严格上界由
  test_bootstrap_scheduler.py 钉住。
- Task 11：真实 `sync-bootstrap --limit-symbols 100 --workers 6`（新增的运维专用抽样开关，
  等距抽样、不改 Universe 语义）。对照跑通：100 只 / fallback 105 次 / 0 超时 / 5 只 BJ source_error /
  satisfied 95 / bootstrap 14s / 6.9 sym/s / 峰值 worker 7 / canonical 2,750 行 / 残留 0。
  崩溃续跑（kill -9 @20s：33 分片、无清单、无整份文件）：续跑零重抓、72 次请求、bootstrap 9s、
  清单补全 100 条、attempts {1:95, 2:5}、**与对照组逐字节相同**、残留 0。
- 真实门禁当场发现并修掉三处（提交 8fd879b）：心跳被 stdout 8KB 缓冲吞掉（改为逐行 flush +
  启动即发首行）；心跳把重试次数当失败只数（改为按标的计数）；崩溃续跑重抓已落盘分片
  （改为跑首压实已落盘分片 + 按实测 bar 数判定 + 补记缺失清单条目 + 跑尾再压实）。
- 未决（Task 12/13 前置）：北交所 920xxx.BJ 在腾讯日线取不到，全市场会命中约 270 只，需要所有者
  决策换端点或显式排除；全市场仍是逐标的请求，按实测约 13 分钟。
- 证据：`docs/REVIEW_NOTES.md` 第十五节；提交 `36993db`（Task 10）、`8fd879b`（修复）、`1e966a7`（门禁）。

## 十六、全市场冷启动执行（Task 13，2026-09-18）

- 授权条件满足（Task 10/11/12 均有显式 PASS 证据）后按所有者"继续"放行，一次跑完、无中断。
- 命令：`astock sync-bootstrap --as-of 2026-09-17 --workers 6`（无 --limit-symbols）。
  预筛 5,301 只 → 455 只已达标未重抓 → 实抓 4,846 只；satisfied **5,008**；short/source_error
  **293**（全部 920xxx.BJ，腾讯日线不支持北交所）；timeout 0；fallback 调用 5,436 次；
  bootstrap 段 **16m06s**、5.5 sym/s（纯抓取 6.5–6.9 sym/s）；压实 3.69s（幂等、字节不变）；
  canonical 167,751 行 / 12.9 MB；暂存 19 MB；exit 1（因为有显式 source_error）。
- 结果：研究池 **453 → 4,935 只**，`NO_LIQUIDITY_MEASURE` 从 5,110 降到 557。
- 未解决：北交所 293 只需所有者决定换端点或显式排除；策略打分仍只有 5 只有分，因为
  `proximity_52w_high` 等需要 252 根 bar，全市场策略长度历史（`sync-research`）尚未跑。
- 证据：`docs/REVIEW_NOTES.md` 第十七节；`docs/ROADMAP.md` 第三节已勾掉全市场日线落地。
