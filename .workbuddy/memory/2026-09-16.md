# 2026-09-16 — a-stock-lens

## 第一条垂直切片交付

分支 `feat/first-vertical-slice`（基于 `main` 的 `c6ecf1c`），7 个提交，PR #2。

链路：`CSV → RawDataset → NormalizedDataset → QualityReport → FactorResult → StrategyResult → Candidate → JSON 快照`，
CLI `astock scan` / `astock factors compute` 与 API `/factors` `/candidates` 均可用。

提交序列：

| SHA | 内容 |
| --- | --- |
| `8b4cb25` | 切片计划 `docs/superpowers/plans/2026-09-16-first-vertical-slice.md` |
| `2fb3494` | `RawPayload` + `LocalCsvProvider` |
| `c3cb28c` | `CsvDailyBarNormalizer` + `ParseFailure` |
| `b0bb43c` | `DailyBarQualityGate` + `QualityReport` + `valid_bars()` |
| `27f7fb8` | `FactorConfig` / `FactorRegistry` / `AverageAmountFactor` |
| `45af39c` | `MomentumScanner` / `Candidate` / `JsonSnapshotStore` |
| `db66ffe` | `run_first_slice` + CLI + API + README |

前置动作：PR #1（bootstrap）以 `--rebase` 合入 `main`。**rebase 合并重写了所有提交 SHA**，
旧台账引用的 SHA（`cd4259b`、`1c1a600`、`a19c5d8`、`537c19f`、`391f6a2`）已失效，
映射表记在 `.superpowers/sdd/2026-09-16-first-vertical-slice/progress.md` 的 Setup 段。

## 本片的关键裁决（后续切片需要遵守）

1. **不实现策略评分**：`score` / `rank_percentile` / `confidence` 保持 `null`，直到权重评审通过。
2. **`next_action` 恒为 `IGNORE`**：设计枚举了动作但没定义选择规则，`IGNORE` 是唯一不误导的默认值。
3. **快照是 JSON 不是 DuckDB**：`SnapshotStore` 协议先定，DuckDB 实现后续按同一协议替换。
4. **窗口不完整即 `NULL`**：不用更短的窗口凑均值。
5. **因子窗口无代码默认值**：必须来自 `configs/factors/*.yaml`。
6. **质量门禁只报告不修复**：单条坏数据 P2 不阻断扫描；整个数据集为空才是 P1。无比例阈值。

## 环境坑

- **`tmp_path` 不可用**：沙箱拒绝往系统临时根 `mkdir`。已加 `tests/conftest.py` 的 `local_tmp` fixture
  （仓库内 `tests/.tmp/`，用后自清理，已 gitignore）。新测试需要 scratch 目录时用它，别用 `tmp_path`。
- **`data/**/*.parquet` 在 .gitignore 里**：fixture 只能用 CSV，用 Parquet 会在干净克隆上静默消失。
- **`git push` 的 HTTPS 通道被拦**（详见用户级 MEMORY.md 的绕道方法）。

## 下一步（下一片）

1. Universe 引擎 + `UniverseSnapshot`（`configs/universe.yaml` 的 `min_average_turnover_20d` 仍是 `null`，阈值待定）
2. 策略评分 / 排名 / `next_action` 路由规则（需先评审权重，写进 `configs/strategies/*.yaml`）
3. DuckDB / Parquet 存储替换 JSON 快照
4. AkShare Provider 接入真实全市场数据
