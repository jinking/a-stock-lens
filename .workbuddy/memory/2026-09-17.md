# 2026-09-17 — a-stock-lens

## 本次会话交付

承接 2026-09-16 的设计一致性实现，本次完成三段：财务数据链路、Scanner 资格判定、权重评审。

### 一、财务数据链路（设计规格 §24 补遗）

按项目所有者批准，把财务三大表的 bulk 源改为**腾讯 WeStock CLI**，理由是实测它同时给出
可靠的 `EndDate`（报告期）与 `InfoPublDate`（公告日期），且支持批量；AkShare/Sina 的
`公告日期` 不是原始公告日（实测同一报告期，资产负债表标 `20260815`、利润表标 `20260417`），
无法单独承载时点规则。

链路：`WestockCliProvider → Raw（逐字表格）→ FinancialStatementNormalizer（49 个指标）
→ FinancialQualityGate → FinancialObservation → 基本面因子`。

关键实现约定：

1. `available_at` = 公告日当天的 A 股收盘（15:00 +08:00），保守取值，宁可晚不可早。
2. 缺失分三类且不互相冒充：源站空值 → `NULL` 观测；值读不出 → 失败记录 + `INVALID`；
   某张表**没落地** → `absent_datasets` 点名。
3. 合并键：名单按 `symbol`、日线按 `(symbol, trade_date)`、财报按 `(code, EndDate)`。
4. 全市场实测（5,576 只 × 三大表）：37 分钟、168 次调用、缺口 4–6 只（退市/B 股，
   项目所有者确认不再处理）；归一化 92 秒、峰值内存约 2.9 GB、0 个阻断问题。

全市场运行暴露出三个只有真实规模才能发现的缺陷，均已修复：

- 源站列集**随批次内容变化**（含银行多 6 列、含保险多 7 列），原实现判为"形状损坏"而整表放弃；
  现按**列的并集**合并，缺失单元格留空（= 缺失，不是 0）。文件合并层同样改为并集扩展。
- 批次成功也可能**静默漏代码**（58/5,576），现对 `missing_symbols` 做一次补抓，缺口降到 4 只。
- 失败原因原先被丢弃，现由 `RawDataset.message` 承载并出现在落地报告里。

### 二、Scanner 资格判定

Growth / Quality / Dividend 三个 Scanner 先只做**资格判定**（设计把权重与评分阈值列为
`Deferred`，打分会是没人评审过的判断）；Value / GARP 因缺估值口径、Industry Trend 因缺行业
数据而暂不实现，注册表会写明各自等待的输入。

### 三、权重评审（同日）

新建 `WeightedPercentileScanner`（权重的**符号表达极性**：负数表示越小越好）与评审工具
`scripts/review_strategy_weights.py`，用真实全市场数据把候选方案的结果摊开。

评审结论：

- Quality 的权重**真的决定看到谁**（等权 vs 收益+现金流优先，top-12 仅 3 个重合）；
  Growth/Dividend 换权重几乎无效（88% 重合），因为被极端值主导。
- 查出两个因子缺陷：`dividend_payout_ttm`、`ocf_to_net_profit` 的分母（TTM 归母净利润）
  接近 0 时比值爆炸，榜首支付率高达 6407%；Growth 榜首 `revenue_yoy` 1819%、
  `net_profit_parent_yoy` 71528%。

### 四、项目所有者裁决（本次已执行）

1. **三个 Scanner 采用等权开启打分**（Quality：`debt_to_asset` 取负号）。
2. **Dividend 改用源站的 `DividendPaidRatio`**（实测正常：茅台 79.00%、平安银行 27.13%），
   不再用 TTM 比值参与排名；原因子保留为证据但不进 `required_factors`。
3. **Growth 暂不做稳健化处理**，该决定保持开放并记录在案。
4. **文档与提交信息一律使用中文**（所有者明确要求）。此约定已写入 `AGENTS.md`，
   从此每次切片都要遵守；历史提交保持英文不改写。

## 后续待办

- AkShare 全市场日线落地（5,564 只，估计约 1.5 小时），跑完才能做全市场日扫。
- Growth 的极端值稳健化口径（缩尾 / 两端同时成立 / 其他）。
- 分红支付率的极性确认：当前取正号（越高表示当年回报越多），负号会偏向留存再投资的公司，
  一行配置即可切换。
- neodata 接研究侧（凭证 12 小时有效，只能由 WorkBuddy 平台刷新）。

## 五、neodata 定位修订（同日，项目所有者指出应按重要性评估）

我原先判断 neodata"只适合候选级"，理由（逐标的 / Markdown / 凭证过期）被实测推翻两条：

- **可批量**：10 只标的一次查询返回 10 行（3.1 秒）；
- **形态不是障碍**：本仓库已为 WeStock 实现了 Markdown 表格解析。

凭证缓存位置已由 v1.6.0 插件迁移到
`~/.workbuddy/plugins/cache/*/finance-data/*/skills/.neodata_token`，旧路径只是兜底；
刷新由所有者在 WorkBuddy 侧完成，Provider 负责如实报告状态。

实测能力（决定它能做哪些主数据）：估值含**历史分位**与**行业相对标签**、逐日股息率/EV/PEG；
行业含**板块估值 + 完整成分股明细**（这是全市场估值的批量路径：按板块迭代而非 5,500 次
单标的查询）；单季财报一次 8 期且**历史行带"最新调整"标记**（时点卫生优于 westock）。

仍然成立的三条限制（写进 Provider 与设计补遗 §24.1）：

1. 意图匹配决定能否取到数据（未命中返回 `1001`），查询措辞固化为模板；
2. **批量回答是部分的**：请求 3 只时利润表只回 2 只、估值只回 1 只，而 `apiData.entity`
   三只都列——覆盖必须按内容判定并补抓一次，不能信 entity；
3. 不做标的枚举，名单仍由 AkShare 提供。

分工定为：**估值 / 行业 / 语义三维度用 neodata 做主数据；财务三大表以 WeStock 为主、
neodata 作交叉验证；名单与日线仍走 AkShare + WeStock。**

本次已实现：`data/providers/neodata.py`（查询模板、批量、缺口补抓、`missing_symbols`、
失败原因入 `message`）、凭证三级解析（环境变量 → 插件目录取最新 → 旧路径）、
`astock doctor` 报告状态、三条真实响应的离线 fixture 与 16 个测试。

下一步：估值与行业数据的归一化层，随后解锁 Value / GARP / Industry Trend 三个 Scanner。

## 六、估值接入与 Value 评审（同日深夜）

### 已实现

- `ValuationObservation`（交易日时点）与 `FinancialObservation` 分开建模；
  分类标签（"相对行业平均估值标签"）进 `text_value`，只作证据、不进排名；
- `data/normalize/valuations.py`：键值头 + 逐日时序表 → 规范估值观测，
  `--`/`暂无数据` → 无值，日期取时序表最新一天（无表时用查询日并计数）；
- 七个估值因子（`pe_ttm`/`pb`/`ps_ttm`/`pe_percentile`/`dividend_yield_ttm`/
  `pcf_operating_ttm`/`peg`）+ 共享 Markdown 解析器（WeStock 与 neodata 共用）；
- Value / GARP 的 `required_factors` 与评审候选方案；两者仍是**资格判定**，权重待批。

### 实测查出的四条限制

1. **估值查询必须逐标的**：10 只一批只回 1–2 只（120 只抽样仅回 23 只），单标的稳定；
   财报查询的批量覆盖是 2/3。
2. **源站"滚动股息率"整列为空**（40 只抽样全 NULL）→ 已从 Value 必需项移除，
   否则 Value 永久不合格。
3. **亏损公司没有 PE/PB**（有 PS）→ 要求 PE/PB 会挡住亏损股，这条判断已记录。
4. **比值可为负，单调极性会奖励负现金流**：榜首出现 `pcf_operating_ttm = -71`、`-759`。
   待所有者决定：排除非正值 / 区间偏好 / 接受现状。另注意到极小盈利公司的
   `pe_ttm` 反复出现整齐的 `5.000`，疑似源站下限。

### Value 评审结果（等距抽样 40 只，34 只合格）

| 候选方案 | 与等权 top-8 重合 |
| --- | --- |
| 等权 vs 便宜优先 | 6/8（75%） |
| 等权 vs 便宜且能赚钱 | 6/8（75%） |

特征化通过：top quintile `pe_ttm` 5.8–10.2 对余下 93–94，`roe_ttm` 1.8–13.9 对 -11.7~-14.3。
**Value 的权重影响远小于 Quality**（那里只重合 3/12）。

### 待办

- 批准 Value 权重（或给出数字）；跑 GARP 评审并定权重；
- 上述第 4 条（负比值）与分红支付率形状，都是"因子形状"决策；
- 行业数据归一化（板块成分已在 fixture）→ Industry Trend；
- neodata 落地与管线接线（估值数据目前只在 Provider/归一化层可用）。

## 七、Value/GARP 权重裁决与负比值政策（同日，所有者"按推荐来"）

### 已执行

1. **Value 取等权**：`pe_ttm -1 / pb -1 / ps_ttm -1 / pe_percentile -1 /
   pcf_operating_ttm -1 / roe_ttm +1`（评审显示三套权重 top-8 重合 75%）。
2. **GARP 取等权**：`peg -1 / pe_percentile -1 / revenue_cagr_3y +1 /
   net_profit_parent_cagr_3y +1 / roe_ttm +1`（评审显示 top-6 重合 83%–100%，
   因为 GARP 的资格约束本身很强）。
3. **负比值政策：比值不存在时判 `NOT_APPLICABLE`**：
   - 源站倍数非正（负现金流/负净资产/负增长）→ `NOT_APPLICABLE`；
   - 基本面比值分母非正（亏损、负权益）→ `NOT_APPLICABLE`（原先报 `INVALID`）；
   - 百分位不受约束（`0.000` 是合法的"最便宜分位"）。

执行后 **6/7 个 Scanner 在打分**（momentum/growth/quality/dividend/value/garp），
只剩 `industry_trend` 等行业数据归一化。

### 新查出的疑点（未解决）

**源站 PEG 值域是 83–1503**（正常 PEG 在 0–5）且会出现负值，说明定义/尺度与常见口径不同。
方向仍可用（越低越匹配），但绝对值不可与外部 PEG 比较。备选方案：用我们已有的
`pe_ttm / net_profit_parent_cagr_3y` 自算——那需要一次口径确认。

## 八、当前整体状态（2026-09-17 收尾）

- 数据：AkShare（名单/日线）+ westock（三大表，含公告日）+ neodata（估值/行业/语义）全部接入 Provider 层；
- 因子：技术/流动性 + 基本面 + 估值共 30 个因子配置；
- 策略：6 个 Scanner 打分、1 个（industry_trend）待行业数据；
- 测试：588 passed，ruff/format/mypy 全绿；
- 未接线：neodata 的落地与 `astock sync/scan` 日常链路（估值目前只在 Provider/归一化层可用）；
- 未做：行业数据归一化、Market Regime/Validation/Signal（阈值 Deferred）。

## 九、neodata 落地与管线接线（同日收尾）

- **落地格式**：`<raw>/neodata/<dataset>/<取数日>.csv`，内容块逐字副本（列 `type,desc,content`）。
  一天一个文件——多行文本合并会破坏溯源；同日重跑覆盖、异日各自留档；
  时点选择 = "不晚于 as_of 的最新一天"，未来文件不可见（有测试）。
- **管线**：新增 `stages.valuation_inputs`，`normalize_stage` 把估值观测并进因子上下文；
  `DatasetIndex` 已按标的切分估值。`source_file` 为 None = 还没同步（与"没有估值"区分）。
- **命令行**：`astock sync --as-of D --valuation --symbol X --symbol Y`。必须显式给 symbol
  （neodata 不枚举 + 估值批量覆盖极低），**缺参数立刻报错**——这条 fail-fast 是一次
  36 秒测试暴露出来的（原实现在抓完几十秒行情后才校验）。
- **真实验证**：落地 2 块 → 230 条观测 → Value 打分：平安银行 83.33（rank 1.0）、
  茅台 66.67（rank 0.5）。`stock` 画像显示 pe_ttm 19.31、pb 6.26、pe_percentile 3.39%。
- 测试 604 passed；CLI 测试文件从 115 秒回到 9 秒（打桩 + fail-fast）。

**下一步**：行业数据归一化（板块成分已在 fixture）→ Industry Trend；随后 Market
Regime/Validation/Signal 仍需所有者确认阈值。

## 十、项目级技能安装：software-company-expert

- 需求：把 `A股盘中监控/software-company-expert-v1.1.0.zip`（WorkBuddy/CodeBuddy 官方专家团
  插件 `software-company@1.1.0`）装到本项目。
- 该 zip 是 CodeBuddy 插件格式：`agents/*.md`（1 Lead + 4 Member）+ `.codebuddy-plugin/plugin.json`，
  **没有 SKILL.md**，不能直接被 Codex 当技能加载，需要做一次格式转换。
- 路径裁决（实测，不是猜）：`codex debug prompt-input` 显示技能根为
  `r0 = ~/.agents/skills`、`r1 = $CODEX_HOME/skills/.system`，
  **`r2 = <repo>/.agents/skills`（项目级）**。故安装位置为 `.agents/skills/software-company-expert/`。
- 落地结构：`SKILL.md`（主理人 SOP、工作流路由、派发方式、与仓库规则的优先级声明）、
  `agents/*.md`（5 个角色提示词逐字复制，SHA-256 与源文件一致）、`agents/openai.yaml`（界面元数据）、
  `references/plugin.json`（原插件清单留档）。
- 裁决：**不**把 5 个角色写成 `~/.codex/agents/*.toml`。虽然 Codex 二进制内含
  `codex_agent_roles` 与 `AgentRoleToml`，但本会话的 `spawn_agent` 没有 `agent_type` 参数，
  装了用不上；改为「派子智能体时让它先读 `agents/<角色 ID>.md`」，并保留无子智能体时的
  串行角色扮演降级路径（降级也必须走完 QA 质量关卡）。
- 边界：技能只提供协作流程与角色分工，**不覆盖** `AGENTS.md` —— 架构依赖方向、财务三时间字段、
  禁止静默兜底、TDD 先失败测试、Deferred 阈值、中文文档与提交信息等规则仍然优先；
  插件的默认技术栈（Vite + React + MUI + Tailwind）仅在用户未指定且项目无既有约定时才作为建议。
- 生效时机：技能目录在会话启动时枚举，二进制里虽有 `skills_watcher.rs`，保守按**新会话生效**处理。
  本会话内该技能不会出现在可用列表里，需要新开对话才能以 `$software-company-expert` 调用。
- 来源许可提醒（沿袭导出文件）：这是腾讯 CodeBuddy 官方资产，仅个人学习/自用，勿二次分发或商用。

## 十一、核心执行链硬化（2026-09-17 深夜，Task 1→10）

按 `Downloads/a-stock-lens-core-hardening-plan.md` 逐 Task 执行，每 Task 独立测试 + 中文提交。
本轮**不加功能**，只把"能跑"变成"只有一个真相"。提交序列：

| Task | 提交 | 内容 |
| --- | --- | --- |
| 1 | `1c30c8f` | 失败测试钉住快照写入权、Candidate 路由、管线顺序三个 P0 |
| 2 | `0bdfad3` | 唯一分析执行链：删 `first_slice.py` / `daily_scan.py`，新增 `pipelines/analysis.py` |
| 3 | `11e1132` | `daily` 为唯一正式 Snapshot writer；其余命令纯计算/预览（含中文文档说明） |
| 4 | `dc37709` | 同日同 kind 内容不同即 `SnapshotConflictError`；内容相同幂等 |
| 5 | `34d5d4e` | Candidate 移到 Market/Signal 之后；上游缺位 → `BLOCKED`，不再假成功 |
| 6 | `b663a11` | 六个策略各自独立 Scanner 类；加权 scanner 降级为 `percentile_scorer` 组件 |
| 7 | `7d58d4c` | 删除"有分数即 WATCH"；建立显式 `CandidatePolicy` 边界 |
| 8 | `ba3ce0e` | `FactorInputRef.available_at` 带真实证据时间；校验器补时点与跨快照检查 |
| 9 | `d964422` | README / ARCHITECTURE / DATA_SOURCES / STRATEGY_SYSTEM / TESTING / ROADMAP 与代码对齐 |
| 10 | 本文件所在提交 | 阶段验证与总结（`docs/REVIEW_NOTES.md` 第十二节） |

### 关键裁决

- **正式快照写入权只有 `daily`**。`factors compute` / `strategy run` / `universe build` 纯计算，
  `scan` 是预览。哨兵快照测试排除了"内容恰好相同"的假通过。
- **同日快照不许静默覆盖**：内容一致幂等，内容不同报错并停止管线。JSON 与 DuckDB 同协议。
- **Candidate 保持 `BLOCKED`**，且理由有两条（上游 Market/Signal 未实现 + 入选规则未批准）。
  两个原因都逐条写进 `daily` 输出与 Job Manifest，而不是并成一句"没有候选"。
- **策略边界回归显式**：`registry.IMPLEMENTATIONS` 是映射表，YAML 的 `weights` 不再决定用哪个类。
  `pipeline/analysis.py` 之前的"两套组装逻辑"全部删除。
- **行为不变的证据**：`tests/fixtures/strategy_parity.json` 由重构前的 `7d58d4c` 生成，
  记录 `generated_from_commit`；重构后逐位比对（实测改一个常数即变红）。
- **时点证据**：`FactorInputRef.available_at` 取真实观测的可用时刻，禁止写成 `context.as_of`
  （那等于自己给自己发前视许可）。校验器独立检查"有报告期就必须有可用时刻"且 `<= as_of`。
- **未替所有者做任何产品决策**：Candidate Qualification（A/B/C/D 四案）、Growth 极值稳健化、
  Dividend payout shape、PEG 值域、Industry Trend 权重、Market/Signal 阈值全部只记录不选择。

### 实测（本轮，非引用历史）

- `uv run pytest` → **674 passed**，0 failed
- `uv run ruff check .` → exit 0
- `uv run ruff format --check .` → exit 0（162 files）
- `uv run mypy` → exit 0（85 source files）
- CLI 冒烟（fixture）：`doctor` / `factors compute` / `strategy run` / `scan` 命令后快照目录文件数为
  **0**；`daily --allow-incomplete` 写出 FACTOR / UNIVERSE / STRATEGY + Job Manifest，
  `MARKET_REGIME` 与 `CANDIDATE` 显式列为缺失，`candidates: 0`。

### 未完成 / 遗留

- 子智能体派发在本环境**两次都没送达任务载荷**（`spawn_agent` + `followup_task` 均无效），
  Task 6 / Task 8 由主 agent 自行实现；下次若要并行，先做一次最小探针确认载荷可达。
- `strategies/eligibility.py` 无生产引用（保留为可登记退路），删或留由所有者定。
- 下一阶段按计划拆成 Plan B（Candidate & Strategy Semantics）/ C（Full-Market Data Scale）/
  D（Market Lifecycle & Product Surface），入口写在 `docs/ROADMAP.md` 第八节。

## 十二、候选资格架构与校准阶段实现（2026-09-17，Task 1→9）

按 `docs/superpowers/plans/2026-09-17-candidate-qualification-implementation-plan.md` 严格执行，逐 Task 单独测试、检查、中文 commit 并推送到远端。

### 提交记录
1. `fdb1bd8` 契约：引入候选资格设计规格与失败测试（Task 1）
2. `3cdef0a` 契约：建立策略资格判定模型与血缘（Task 2）
3. `860dddb` 策略：实现六策略独立资格判定器与注册表（Task 3）
4. `933eadd` 政策：实现候选证据模型与横截面代表性选择（Task 4）
5. `8ab1588` 候选：持久化资格证据与策略版本（Task 5）
6. `ae4882a` 管线：接入策略资格层并保持候选发布阻塞（Task 6）
7. `07f4e01` 校准：建立候选资格全市场证据报告（Task 7）
8. `245f456` 命令：增加只读候选资格校准报告（Task 8）
9. 本次提交：审计：完成候选资格架构与校准阶段验证（Task 9）

### 核心裁决与架构落地
- **批准架构**：Top 10% 相对分位数底线（`rank_percentile >= 0.90`）与独立绝对质量门槛双门槛；每策略软保底 3 只、全局上限 50 只、绝不硬凑 20 只下限；`MarketValidation.CONTRADICTED` 一票否决；严禁跨策略综合打分。
- **产品安全阻塞**：六个策略绝对门槛未获所有者批准前，生产配置不创建任何虚假规则；`daily` 保持 `BUILD_CANDIDATES` 为 `BLOCKED`。
- **只读校准工具**：`astock calibrate candidates` 采用非持久化分析链计算全市场分布证据，保证 Snapshot / Watchlist / Job 目录 0 变更，输出带显式警告的 JSON 与 Markdown 校准报告。
- **独立审计升级**：`tests/artifacts/validator.py` 升级支持候选资格与选择政策校验，保持 0 内部代码导入。

## 十三、真实数据链路验证与本机 WeStock 二进制接入（2026-09-17 收尾）

按所有者指示，先用 5 只真实标的验证链路，再把本机二进制接进来。全程未发明任何阈值。

### 二进制为什么此前"缺失"

`providers/westock.py` 的 `DEFAULT_BINARY` 是**仓库内相对路径** `tools/bin/westock`，而本仓库
从来没有这个目录，`README.md` 也明写"二进制不随仓库分发"，于是 `doctor` 一直如实报
`[unavailable]`。二进制实际躺在同机另一个项目 `~/Documents/ChatGPT/A股盘中监控/tools/bin/westock`
（`westock 0.0.4`，Mach-O x86_64，8.6MB，SHA256 `0c025ab4…`）。

**裁决**：拷到默认路径 `tools/bin/westock`，不设 `ASTOCK_WESTOCK_BIN`——默认路径就是为
"本机装好即被发现"设计的，环境变量留给别的安装位置。二进制不入库，只留 `tools/bin/.gitkeep`。

### 顺手补的两条忽略规则（依据 `docs/DEVELOPMENT.md` 第 38 行"`data/`、`var/` 不进版本库"）

- `data/raw/**`：真实 `astock sync` 会写 `daily_bars.csv` / `securities.csv` / 三大表到这里；
- `tools/bin/*`：外部二进制，可从官方发布地址复现获取。

补之前，一次真实落地会让 `git status` 多出 22,693 行日线与 5,565 行名单两处未跟踪文件。

### 修掉一个依赖开发机状态的测试

`test_doctor_reports_the_financial_statement_provider` 删掉 `ASTOCK_WESTOCK_BIN` 后断言
`westock-cli [unavailable]`，隐含假设默认路径不存在——**装好二进制后它反而必红**，测的是开发机
安装状态而不是 provider 行为。失败证据在先（`assert 'westock-cli [unavailable]' in …[ok]…`），
改为显式把 `DEFAULT_BINARY` 指到不存在的文件后再断言。修复后 724 passed，
`ruff check` / `ruff format --check` / `mypy`（99 源文件）全绿。

### 真实数据实测（2026-09-17，5 只：000001.SZ / 300750.SZ / 600519.SH / 601318.SH / 688981.SH）

- **落地**：日线 22,693 根（含 1991 年至今全历史）；名单 5,565 只（SSE 2,320 / SZSE 2,901 / BSE 344）；
  三大表各 40 行（5×8 期），`EndDate` 与 `InfoPublDate` 都在（茅台 2026-06-30 / 2026-08-15）。
- **增量规则**：同日重跑 `sync` → 0 行写入、已落地标的全部 skip。
- **因子覆盖**：财报落地前 20/120 有值 → 落地后 78 → 接入 neodata 估值后 **89/120**
  （9 NULL、22 NOT_APPLICABLE，无一处兜底成 0）。
- **六策略**：momentum 5/5、dividend 5/5、growth 5/5、quality 3/5、value 2/5、garp 1/5。
- **估值覆盖实测**：`sync --valuation` 请求 5 只只回 2 只（000001.SZ、600519.SH），
  印证 `README.md` 里"10 只只回 1–2 只"的记录；其余 3 只的 7 个估值因子保持 `NOT_APPLICABLE`。
- **时点规则有效**：为 2026-09-16 跑 `daily` 时，09-17 落地的估值文件**没有被读到**
  （该日快照里 7 个估值因子全是 `NOT_APPLICABLE`）。
- **同日覆盖保护有效**：财报落地后重跑 09-17 被 `SnapshotConflictError` 挡住
  （`FACTOR snapshot for 2026-09-17 already exists with different content`），旧快照未被改动。

### 09-17 正式快照重建

所有者确认后，把上午（财报落地前）那份 09-17 的 FACTOR / UNIVERSE / STRATEGY 备份到
`/tmp/astock-0917-presnapshot-Hw5mXG` 再删除，重跑 `daily --as-of 2026-09-17 --allow-incomplete`
→ 写出三份快照、`5 blocked, 0 failed`、`candidates: 0`、缺失 MARKET_REGIME 与 CANDIDATE。
09-04 与 09-16 两份快照未被触碰。

### 遗留（都不是代码问题）

- **行业映射来源缺失**：`astock calibrate candidates` 需要 `--industry-map`（symbol,industry CSV），
  仓库里没有生成器，行业数据来自尚未接线的 neodata。这是"六策略绝对门槛"校准报告当前的真实前置。
- **全市场落地规模**：实测 1.82s/只串行 ≈ 2.7 小时/全市场，与 Plan C 的 10 分钟目标仍有距离。
- **未提交决策**：`data/snapshots` 属运行时产物，09-17 的快照重建是显式操作而非自动覆盖；
  是否需要"重建某日快照"的正式命令，留给所有者。
