# 2026-09-26

## 测试精简切片：收敛至 1038 例并收口（2026-09-25 开工，2026-09-26 收口）

### 背景与验收线

规格 §7.1 验收线要求把测试用例总数压到 ≤1040 例（原 1298）。切片纪律：**只动 `tests/`**、生产零改动（`src/**`、`configs/**` 零 diff）、冻结 5 个未提交测试文件一行不改、零断言删除、零新增测试、不引入 `skip` / `xfail` / `importorskip`。

- **用例数**：1298 → **1038**（−260；余量 2）。
- **含用例文件**：136 → **66**；`tests/**/*.py` 141 → **71**（71 中 5 个无用例：`conftest.py`、`support.py`、`tests/artifacts/__init__.py`、`tests/artifacts/validator.py`、`tests/artifacts/trade_gate_validator.py`）。
- **批次**：0 基线（1298）→ 1 参数化收敛 −72（1184）→ 2 近邻家族 −96（1077）→ 3–4 去重与清理 −14（1063）→ 5 文件合并（0）→ 6 文档治理（0）→ 7 备用池收口 −25（**1038**）→ 8 全量验收（净 0）。

### 三条合并规则与治理固化

1. **遍历全部成员**：参数化/表驱动必须覆盖原用例全部成员，合并前后逐成员对照，缺一即失败（不许抽样抽稀）。
2. **先收集再断言**：先收集全部输入再统一断言，禁止中途短路导致后面的断言实际未执行。
3. **失败消息点名参数**：表驱动用例失败时必须带参数身份（参数 id/用例名），否则从消息无法定位是哪一行数据。

已固化进 `docs/ARCHITECTURE.md` §20.5 的两条治理：**「表驱动优先」**（同族断言优先收敛为表驱动）与**「数量上限」**（测试数量上限作为门禁口径）。这是本切片留在仓库内的规范增量，不只是过程记录。

### 最终实测（证据在 `var/test-consolidation/`）

- 本地全量 `1038 passed, 7 warnings` **两次**（304.03s / 293.26s，无并发；`task-14-pytest-step1.txt`、`coverage-after-2.txt`）。
- 覆盖率终值 `TOTAL 8703 734 92%`（`--cov=astock_lens --cov-report=term`）与基线**逐字相同**，未低于基线。
- `src/`、`configs/` 零 diff；冻结 5 文件零改动；`ruff check src tests` 全绿。
- 远端 CI **未拿到全量**（见下）。

### 批次与 commit（台账 §8 已登记）

| 批次 | 任务 | commit | collected |
| --- | --- | --- | --- |
| 0 基线 | Task 0 | `872e99b` + `59119d2` | 1298 |
| 1 参数化收敛 | Task 1–4 | `b466794` `95032f7` `50a8ca9` `c3957b3` | 1298 → 1184 |
| 2 近邻家族 | Task 5–9 | `ae5bb62` `bd285ba` `889c652` `0c6426b` `8b5cb8c` | 1184 → 1077 |
| 3–4 去重与清理 | Task 10–11 | `55398f3` `20baf65` | 1077 → 1063 |
| 5 文件合并 | Task 12 | `a9ec8f7`（修复轮 `48d5e1e` `9c1a39c`） | 1063 → 1063 |
| 6 文档治理 | Task 13 | `823c272` | 1063 |
| 7 备用池收口 | Task 15 | `37644c1` `7dacdd2` `fb20597` `7432669` `4745bab` `b941560` `9a63903` `b94eb8a`（文档 `fca6e88` `a3817a7`、修复轮 `329ac5c`） | 1063 → 1038 |
| 8 全量验收 | Task 14 | `7288fd0` `a4130d2` `2a72965` | 1038 |

### 验证手法（「零断言丢失」的自证形态，可复用）

- **逐文件计数器**：合并前后按文件统计 collected，`1063 → 1038` 的 −25 必须只出现在 Task 15 动过的 8 个文件上（`test_cli_entry_flows.py` 61→55、`test_candidate_stage.py` 41→39、`test_api.py` 36→35 等），逐文件对上才算通过。
- **preservation check 与 negative control**：每次合并写独立脚本（`var/test-consolidation/task-*-preservation-check.py`、`task-*-negative-control.py`），先证明旧断言族在合并前全过，再证明新表驱动覆盖同一成员集合。
- **字面片段与异常类型探针**：`pytest.raises` 的 match 字面片段逐条做 HEAD 存在性检查——终审据此给出归因：计数 194 → 155（−39），77 处随 90 个被合并源文件迁入域文件、存活文件内自持数 117 → 155，属表驱动收敛而非断言丢失（详见台账 §11 末行）。
- **7 条 warning 身份**：1 条 starlette/anyio 弃用 + 6 条 Python 3.14 fork 弃用（后者远端 Python 3.12 不出现），均与本切片无关。

### 关键裁决（供后续会话继承）

- **R9**：文件数 **66** 而非 ≤65——在不违反规格 §6 no-merge 名单的前提下无法到 65。按裁决记录实测数、不强行合并，保留 `tests/unit/test_qualification_config.py`。
- **远端 CI 格式漂移**：run `36202187215` / `36202478208` 红在 `ruff format --check`，点名两个 **main 继承**文件（`src/astock_lens/data/providers/akshare_provider.py:454`、`tests/unit/test_akshare_provider.py:356`；与本分支逐字节相同，blame → main `3c97c25`），其后 `mypy`/`pytest` 步被 Actions 跳过。修复属 **main 的一次性格式提交**，**待所有者裁决**；本分支不得改（约束禁止动冻结文件与 `src/`）。
- **`var/test-consolidation/` 去留**：未跟踪证据目录是否加入 `.gitignore` 待所有者决定。
- **`mypy` 对 `tests/` 不生效**：`pyproject.toml` 的 `files = ["src/astock_lens"]`，对测试目录只是形式门禁（已裁决不改配置）。
- **终审结论**：0 Critical / 0 测试代码缺陷，`tests/` 无需再改动；2 个 Important（PROGRESS 停在 Task 13 时点、memory 缺本切片条目）与 1 项可执行 Minor（台账 §11 补 `pytest.raises` 归因）已由 2026-09-26 修复轮处置。

### 已知遗留（不阻塞）

- **已接受未修的 Minor 清单**（Task 1 F2/F3、Task 2 ×4、Task 5 无行数守卫、Task 7 两处未申报增强、Task 12 R1/R2、Task 14 ×3 等）与全部过程裁决只存在于工作区级 `.superpowers/sdd/2026-09-25-test-suite-consolidation-implementation-plan/progress.md`，**不随仓库交付**——worktree 移除即失，故本文件承担仓库内继承面。
- 延期项：`tests/unit/test_bootstrap_progress.py:27` 的 `ROOT`、§10 加固项、`var/` 保留决定、CI 例外。
- 既有 11 处 `pytest.importorskip("duckdb")` 与 base 同数，非本切片引入（终审 Minor-6，不修）。

### 指向

- 台账（仓库内）：`docs/superpowers/reviews/2026-09-25-test-consolidation-audit.md` —— §7.2 检查点 / §8 执行记录 / §9 文件映射 / §10 备用池审计 / §11 Task 14 收口。
- 规格：`docs/superpowers/specs/2026-09-25-test-suite-consolidation-design.md`。
- 证据目录（未跟踪）：`var/test-consolidation/`（`coverage-after-2.txt`、`task-14-pytest-step1.txt`、`task-14-collect.txt`、`task-15-perfile-at-head.txt` 等）。
