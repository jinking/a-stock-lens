# 2026-09-19

## 收尾：研究基线三任务交付完成（承接 2026-09-18 第二十一节）

- 计划 `docs/superpowers/plans/2026-09-18-research-baseline-implementation-plan.md`
  的任务 **1.1 / 1.2 / 1.3** 全部完成，产物落在 `var/acceptance/baseline-20260918/`，
  详细结论、验证证据与实测坑见 `.workbuddy/memory/2026-09-18.md` 第二十一节，不重复。
- **三条本地提交（不 push）**：
  - `0111eea` 建立研究数据与正式状态的离线审计基线（1.1）
  - `50c3a96` 在校准报告中保存行业映射来源与覆盖缺口（1.2）
  - `bd5a93a` 固化迁移前的研究分析与校准基线（1.3）
- **关键数字（供迁移后逐字节比对）**：
  - 输入清单 5,058 个白名单文件 / 300,127,558 字节；`problems` 为空
  - 研究池 **2,303**；行业覆盖 2,294/2,303 = 0.9961；缺口 **9** 只
  - 四榜可打分数：growth **2,302** / momentum **2,281** / quality **1,697** / dividend **1,612**
  - 1.3 六产物：`factors.jsonl` 55,272 行、`strategies.jsonl` 13,818 行、
    `research-universe.json` 2,303 只、`calibration.json` / `calibration.md` / `manifest.json`
  - 四类数据 5,021 个文件 sha256 **零变化**；正式状态 12 个文件 sha256 **零变化**
  - 全量 `uv run pytest -q` → **890 passed**，0 skipped（必须串行独占跑）
- **BLOCKED.md**：本轮无阻塞；5 项"顺手活"（含开机即红的
  `ruff format --check .` → `tests/unit/test_bootstrap_sync.py:158`）故意未做，理由已写明。
- **验收要点提醒**：验收会复用 `var/acceptance/baseline-20260918/logs/` 下的原始输出；
  迁移后用同一 `scripts/audit_research_baseline.py` 重跑，对四类数据 sha256 做逐字节比对。

## 二阶段·第一刀：存储迁移 v2 的任务 2.1 + 2.2

计划 `docs/superpowers/plans/2026-09-18-storage-migration-v2-implementation-plan.md`
的任务 **2.1（统一路径解析）+ 2.2（归一化读取边界）** 完成，两条本地提交、不 push。
详细证据见 `PROGRESS.md` 的"二阶段 · 第一刀"一节与 `docs/REVIEW_NOTES.md` §25 / §26。

- **两条提交**：
  - `6a824fe` 统一分析与业务存储的有效路径解析（2.1）
  - `提取归一化数据读取边界并复用单次分析输入`（2.2，CJK 主题，未加 conventional 前缀）
- **完成条件全达成**：2.1 验收 28 passed；2.2 验收 46 passed；全量
  **917 passed / 0 skipped / EXIT=0（582.30s）**。
  917 = 第一步基线 890 + 本切片新增 27（16 + 11），数字自洽、无跳过无放宽。
- **行为不变硬证据**：重跑 capture 后 `factors.jsonl` / `strategies.jsonl` /
  `research-universe.json` 三份 sha256 与迁移前基线**逐个 SAME**
  （`2a507477…`、`15ce4f25…`、`fe817691…`）；5,058 个白名单文件指纹零漂移。
- **刻意没做的四件事**（等裁决）：改 `snapshots/duckdb_store.py` 的
  `CREATE TABLE IF NOT EXISTS`、删 `data/normalized/` 旧 4 个 `.parquet`、装
  `pyarrow`/`polars`、改任何业务阈值。

### 两个会重复踩的实测事实（重点，别忘）

1. **`RawDataset.fetched_at` 是墙钟**，所以两次 `normalize_stage()` 的返回值**永不
   相等**。计划 2.2 给的 `assert actual == expected` 按字面过不去。正确口径是比对前
   递归摘掉 `fetched_at` 这**一个**字段、其余逐项比对（不是放宽断言，留着它等于断言
   两次调用在同一微秒）。以后任何"比对两份 `NormalizeOutcome`"的代码都要先决定要不要
   看这个字段。
2. **本机跑全量 pytest 必须"无沙箱"**。沙箱注入的 `sitecustomize` 会制造两类**假
   ERROR**，与仓库代码完全无关：
   - `tmp_path` 相关的 4 条用例：沙箱的 `Path.mkdir` 补丁在 `exist_ok=True` 时跳过
     存在性短路，直接向 broker 发 mkdir，broker 回 `EEXIST` →
     `PermissionError`（根因是补丁破坏 `exist_ok` 语义，不是临时根不可用）；
   - 删除 shim 给每条删除起一个 **Node 子进程**做批量删除守卫，`local_tmp` 每用例
     teardown 删一个 `tests/.tmp/<uuid>`，上万次 → 又慢一个数量级、且整批被标 ERROR。
   - 对照：同一文件默认跑 `9 passed + 9 ERROR`；`CODEBUDDY_SAFE_DELETE_ENABLED=0` 跑
     `9 passed / 27.82s`；无沙箱全量 `917 passed / 582.30s`。
   - 教训：**沙箱内的 pytest ERROR 一律先假定是环境问题**，别急着怀疑自己的改动。
     本轮为这个误判白追了约一小时。

## 跨会话进展核查（22:10 → 次日 16:41，由另一会话执行、本次会话只读复核）

**结论：v2 计划 8 个任务完成 2 个（2.1 / 2.2），另加前置第一阶段 3 个任务。
数据一个字节都没搬**——2.1/2.2 的验收目标恰恰是"行为不变"，明确写"不发布 Parquet
布局、不切默认后端、不建生产 DuckDB"，属于为迁移铺路的结构重构。

- 完成：`0111eea`/`50c3a96`/`bd5a93a`（第一阶段基线）、`6a824fe`（2.1 路径解析）、
  `cee456f`（2.2 读取边界）、`e1713e6`（记录装 PyArrow）。全量 917 passed / 0 skipped。
- 未开始：2.3 完整证据编码与 Parquet 原子发布、2.4 独立比对与两路分析、
  2.5 DuckDB Store 修正 + JobStore、2.6 历史迁入与回滚出口、2.7 真实全量验证、
  2.8 生产切换。
- 现状核对：`data/normalized/` 仍是 9-18 我做的旧扁平布局（4 个 parquet +
  `quality_findings.json`，时间戳 9-18 23:26–23:27），v2 要的
  `data/normalized/v1/<as_of>/<bundle_id>/` 数据包**不存在**；默认读取仍是 CSV；
  三个业务后端默认仍是 `json`；`var/astock.duckdb` 还是 5.78 MB 的旧库
  （4 张实体表 + 9 个视图，`migration_verification` 22 PASS / 0 P0）。

### 本次核查发现的隐患（尚未处理，等金总裁决）

1. **`uv.lock` 有 1084 行未提交改动，整份文件被换成阿里云镜像源**：1069 处
   `files.pythonhosted.org` → `mirrors.aliyun.com/pypi/simple`，且 `size` /
   `upload-time` 元数据被删光。成因是装 PyArrow 时网络不通、改走镜像所致。
   提交与回滚都有代价：提交＝把镜像源锁进版本库且丢元数据；回滚＝`pyarrow 25.0.1`
   的 lock 条目会一起没掉（它是靠新 lock 才记下来的）。**这是当前工作区唯一的
   未提交改动，下次提交很容易顺手带进去。**
2. **`polars` 声明了但装不上**：`pyproject.toml` 的 `data` extra 含 `polars>=1.0`，
   lock 里有 1.44.2 条目，但环境 `ModuleNotFoundError`。而 `uv run`（不带
   `--no-sync`）会尝试补齐 → 可能又卡在网络下载上绊住下一次会话。
   缓解：当前代码库无任何模块 import polars（2.3–2.6 只需要 PyArrow）。
3. **`data/normalized/` 的旧扁平 4 parquet 是"孤儿"**：`BLOCKED.md` 已声明它
   "不是迁移已完成的证据"，但没删（等目录布局裁决）。风险是后来人误读成迁移已完成。
4. **CLI 现在依赖 `configs/` 可读**：`_store()`/`_job_store()` 会经
   `resolve_storage_paths()` 加载 `configs/app.yaml`，在无 `configs/` 的目录跑 CLI
   会失败，除非设 `ASTOCK_CONFIG`（见 REVIEW_NOTES §25.2）。
5. 另一个会话的 memory 写"不 push、只本地提交"，但实测 `origin/main` 已同步到
   `e1713e6`（`git rev-list --left-right --count origin/main...HEAD` = `0 0`）。
